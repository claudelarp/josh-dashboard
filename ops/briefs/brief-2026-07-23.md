# Brief — Thursday 23 July 2026

*(First brief — generated inline as the P2 test. Scheduled 06:30 runs start once you install the job: one command, see the morning handoff.)*

## Today

No calendar events today or in the next 14 days on `joshpnieman@gmail.com`. If your class schedule, training, or client calls live somewhere else, add that calendar — the brief only sees what Google Calendar sees. School section suppressed (summer break).

## Needs a decision

1. **Current drop facts missing** — you confirmed a drop in sampling/production, but its name, launch date, sample round, and blockers aren't recorded. 5 minutes: `ops/drops/inputs/drop-brief-current.md`. Until then the system can't track what is currently your #1 priority.
2. **All 12 store variants have zero inventory and no SKU codes.** Consistent with being between stocks mid-drop — but when inventory lands, assign SKU codes (scheme of your choice, note it in `ops/context/terminology.md`); until then, everything keys on product names.

## Belated

Yesterday (Jul 22): 0 orders, $0.00 net. Inventory: 0 units on hand across all 12 variants at "Shop location" (see decision item 2). Content slot: no calendar yet (P3 pending Notion export). Days-of-cover: insufficient history — needs ~2 weeks of sales data.

## Health

Not seeded yet (P4 — pending your protocol sitting). No refills or appointments tracked.

## Open loops

None older than 5 days (the vault is one day old). Morning handoff tasks are in `ops/tasks.md`.

## Stale data

Nothing stale — but most sources are **not seeded yet**, which is different: `content-calendar.json` (P3), `refills.json`/`appointments.json` (P4), `drops.json` (your 5-minute fill), `sku-performance.json` (P5). Commerce data is fresh as of last night's pull.
